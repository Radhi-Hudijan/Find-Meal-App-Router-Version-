import createHintView from "../views/hintView.js";

function createHintPage() {
  const props = {
    title: "About Find Meal APP",
  };

  return createHintView(props);
}

export default createHintPage;
